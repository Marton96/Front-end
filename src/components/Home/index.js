import React from 'react'

const Home = () => (
  <div>
    <h1>Are you looking for a new job?</h1>
    <h2> First of all REGISTER or if you already have an acoount LOG IN!</h2>
  </div>
)

export default Home
